{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "switchlynode.serviceAccountName" . }}
  labels:
{{ include "switchlynode.labels" . | nindent 4 }}
{{- end -}}
